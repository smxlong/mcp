# Changelog

## Unreleased

- Add the ten ScoreTex v1alpha1 MCP tools, exact rational score graph, immutable
  revisions, atomic persistent edits, selectors, and idempotent retries.
- Add required transformations, validation profile, semantic diffs and canonical
  JSON rendering for the documented notation subset.
- Ship repeatable stdio validation, MCP integration and concurrency tests,
  container build, and stdio/Streamable HTTP command.
